#take a (series of) text, pass each word through analyzer, write each word on a line with preceding, following context, outcome of analyzer, index in sentence, sentence length (so reverse indexing is possible), index of sentence, index of text
